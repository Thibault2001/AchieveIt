module.exports = {
    presets: [
      '@babel/preset-env', // Preset for compiling JavaScript
      '@babel/preset-react' // Preset for compiling React JSX
    ],
    // You can add more plugins or configure Babel further if needed.
  };
  